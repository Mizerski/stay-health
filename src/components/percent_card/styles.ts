import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    width: "100%",
    height: 102,
    borderRadius: 8,

    alignItems: "center",
    justifyContent: "center",
  },
  navigation: {
    position: "absolute",
    right: 10,
    top: 10,
  },
});
